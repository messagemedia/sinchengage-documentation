title: Campaign
required:
  - template_id
  - message
type: object
properties:
  template_id:
    type: string
    description: Template to use for the landing page
  parameters:
    type: object
    description: Parameters to use for the landing page and the message content
  message:
    $ref: ../MessageTemplate.yaml
