{
  "name": "sinchengage",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "redocly lint",
    "inject": "node scripts/inject-code-samples.mjs",
    "bundle": "redocly bundle engage -o web_deploy/openapi.yaml",
    "preview-docs": "npm run build && npx --yes serve web_deploy -l 8080",
    "build-docs": "redocly bundle engage -o .tmp/openapi.bundled.yaml && npm run inject && redocly build-docs .tmp/openapi.injected.yaml -t web/index.html -o web_deploy/index.html",
    "build": "npm run build-docs && node scripts/copy-assets.mjs",
    "test": "npm run lint"
  },
  "devDependencies": {
    "@redocly/cli": "^2.0.0",
    "js-yaml": "^4.1.0"
  }
}
